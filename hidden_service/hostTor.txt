python3 -m http.server --bind 127.0.0.1 8080

whereis tor
cd /etc/tor
ls
sudo nano torrc
uncomment tor hidden service
then change port 8080
bootstraping tor
cd /var/lib/tor/hidden_service
ls
cat hostname
